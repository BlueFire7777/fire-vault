# FIRE 開発タスク運用ルール(v3.3 対応)

本書は、FIRE 統合要件書 v3.3 の開発を Git + Obsidian + .md で運用するためのルール集。

## 1. タスクID採番ルール

**正本: [TODO管理表 Google Sheets](https://docs.google.com/spreadsheets/d/1Zf0rmQHxuBLWsBriV3hKH51eGiJ5u96b/edit?gid=1782363065#gid=1782363065)**

- 全タスクは F001 〜 F999 の連番で Google Sheets の TODO_Master シートに一意採番
- **Google Sheets を Single Source of Truth (SSoT) とする**。ローカルxlsxファイル(FIRE_開発TODO_v8.xlsx)はバックアップ位置づけ
- 新規追加時は末尾に追番
- タスクIDは Git commit、Branch名、PRタイトル、Obsidianノート名すべてで必ず引用する

### 管理の二重化を避ける原則

- ステータス更新 (未着手→実装中→完了) は **Google Sheets でのみ行う**
- Obsidian のタスクノートは「タスクの詳細な実装メモ・ログ」を書く場所
- Obsidian の frontmatter の `status:` は参考情報であり、Google Sheets 側が優先

## 2. Git 運用

### ブランチ戦略

- main - 本番運用ブランチ
- dev - 統合開発ブランチ
- feature/F242-openclaw-setup - タスクID含むfeatureブランチ
- hotfix/FXXX-bug-name - 緊急修正

### コミットメッセージ規約

`<type>(<task_id>): <subject>` の後に本文、最後に Refs: F001, F242 等

type: feat / fix / docs / refactor / test / chore

### FIREリポジトリ個別の Git 設定

StudioZ業務と分離:

```
cd ~/fire
git config user.name "BlueFire7777"
git config user.email "<個人Gmail>"
```

### PRルール

- PRタイトル例: `[F242] OpenClaw セットアップ`
- PR本文にタスクID、完了条件、テスト方法、関連Obsidianノートリンク
- マージ時に Google Sheets TODO_Master のステータスを完了に更新

## 3. Obsidian 運用

### Vault 構造

```
~/fire-vault/
  00_index/README.md          トップページ
  01_requirements/            要件書40章(v3.3) + _index.md
  02_todo/                    タスクごとのノート
  03_design/                  設計書
  04_daily/                   日次メモ
  05_weekly_review/           週次レビュー
  06_monthly/                 月次レビュー
  07_incidents/               障害・事故
  08_reference/               参考資料
  タスク運用ルール.md          本書
```

## 4. 崩してはならない前提(v3.3: 9項目)

1. FIREのエッジは**再現性優位**
2. Pattern Storeは**大量蓄積 + 階層管理**
3. パターンは段階検証を通過して昇格(**Stage飛ばし禁止**: 0→1→2→3)
4. **Evaluation Agentは提案のみ**(自動反映禁止)
5. Mac mini常駐ランタイム前提(**OpenClawで実現**)
6. UIはレトロ炎テーマ
7. 初期本番はデイトレ主軸
8. **(v3.3)全自動は目指さない。Live Advisory(半自動)が FIRE の完成形**
9. 完全放置はしない(監督者付き自律運用)

## 5. 開発着手順(第40章の絶対原則)

1. OpenClaw 基盤が最初(全ての土台)
2. Pattern Store / Feature Store が最優先(エッジの本質)
3. LINE 通知基盤は並行開発可
4. 約定取込は M+2 以降(初期は LINE 手動報告)
5. Stage 飛ばし禁止

## 6. 環境情報(2026-04-24時点)

- 開発マシン: Mac mini M4 Pro(64GB, 12core/16GPU, 1TB SSD)
- OS: macOS
- ユーザー: bluefire
- FIRE Vault: ~/fire-vault
- FIREコードリポジトリ: ~/fire(F004で作成予定)
- GitHub: BlueFire7777/fire(Private)
- Claude: 個人アカウント(Pro プラン、v3.3 方針で運用)

## 7. Vault Git 運用ルール (2026-05-03 確立)

### 基本方針

`~/fire-vault` は **GitHub: `BlueFire7777/fire-vault` (private, main branch)** で管理する。
Mac mini 障害時の復旧と Karpathy 式 `log.md` の時系列追跡のため、Vault 更新は必ず GitHub に push する。

### 必須ワークフロー

Vault に変更を加える全タスクで以下を完了条件とする:

1. ファイル編集 (要件書追記 / 新章作成 / log.md 追記 / TODO メモ等)
2. `git add -A` (または変更ファイルを明示)
3. `git commit -m "<type>: <scope> ..."` (Conventional Commits 形式)
4. **`git push origin main`** ← これを忘れない
5. `git log -1` で最新コミット確認

### コミットメッセージ規約

- `docs:` 要件書・design ノート・log.md 等のドキュメント変更
- `feat:` 新機能 (主に code 側で使用、Vault では稀)
- `fix:` 誤記訂正・矛盾修正
- `chore:` `.gitignore` / 設定ファイル等のメタ変更

例:
- `docs: v3.4 空売り対応パッチ適用 (12章追記 + 第42章新設)`
- `docs(02_todo): F230 完了メモ追加`
- `docs(04_daily): 2026-05-03 の作業ログ追加`
- `chore: .gitignore に secrets パターン追加`

### 分割コミット原則

- 設定変更とコンテンツ変更は別コミット
- 複数 F-task のメモは backfill として別コミット
- v3.x 等の仕様変更は単独コミット
- 日次の daily ノートも別コミット

### .gitignore で除外しているもの

- `.env` / `.env.*`
- `*.key` / `*.pem`
- `*credential*` / `*token*` / `*.secret`
- `.obsidian/workspace*` (個人の表示状態)
- `.DS_Store`
- `.claude/` (kepano/obsidian-skills のクローン、別リポジトリ管理)

### 機密情報チェック

新規ファイル追加時、コミット前に以下で機密混入を確認:

```bash
git ls-files | grep -iE '\.env|credential|token|secret|password|jquants_api'
```

ヒットした場合は `git rm --cached` + `.gitignore` 追記で対処、push しない。

### 復旧手順 (Mac mini 障害時)

1. 新環境で `git clone https://github.com/BlueFire7777/fire-vault.git ~/fire-vault`
2. Obsidian で `~/fire-vault` を Vault として開き直す
3. `.gitignore` で除外されている `.env` 系は別バックアップから復旧 (1Password 等)
