# F276 Phase 1: events ≥ 50 達成のための構成判断材料 (調査 + 推奨案選定)

**作成日**: 2026-05-05
**起点**: F275 完了 (`fire` 性能目標完全達成、events_total = 0 のまま F276 移管)
**起点 commit**: ~/fire main HEAD = 156b879 (F300 + F300-race 完結後)
**目的**: Phase 2-A/B/C 構成判断材料を全数列挙し、本部 Q 4 件の判断根拠を集約。
**本 Phase は調査 + 推奨案選定のみ、コード変更禁止 (実装は Phase 2 以降)**。

---

## 1. F276 目的 + 完了条件再掲

### 1-1. 起点状況 (F275 Phase 1 で確定した score 構造的限界)

72030 / 2026-04-30 評価実測:

```
score = 0.20 × 1.0      (similarity max)
      + 0.25 × 0.5      (win_rate, positions 空 default)
      + 0.20 × 0.5      (expected_value, default)
      + 0.10 × 0.5      (regime_fit, F104 未稼働 default)
      + 0.10 × 0.5      (fill_quality, default)
      + 0.10 × 0.1867   (priority_boost, active=1 のみ)
      + 0.05 × 0.7      (risk_fit)
      = 0.5787 < SCORE_THRESHOLD_EXECUTE (0.65)
```

→ events_total >= 50 達成には 3 サブスコープが必要 (Phase 2-A/B/C)。

### 1-2. F276 完了条件 (F276 todo より)

- Run a (1340 tick × Core500) で events_total >= 50 達成
- event_counts 内訳 (candidate_extracted / entry / exit / force_close / halt) 非ゼロ
- paper_live_results / paper_live_positions が Run a 由来件数で非ゼロ
- F053 promotion check で 5/5 PASS (実 PASS、偽 PASS でない)
- Stage 3 移行 (F266) ゲート条件 events 部分を満たす

---

## 2. Vault grep 結果 (F271 v1.2 §3.6 事前 grep 観点 v3 適用)

### 2-1. 関連設計記録 (~/fire-vault/03_design/)

| ファイル | 主要発見 |
|---|---|
| `F040_backtest_status_2026-05-04.md` | F040 simulation/backtest/ 既実装、20 PASS、過去 1 run 履歴あり (2026-01-01〜2026-04-30) |
| `F275_phase1_design_2026-05-04.md` | per-call cProfile 内訳、SQL 40/call + sector_filter 4,700 loop が真因 |
| `F275_similarity_optimization_complete_2026-05-04.md` | per-call 9.81 → 2.53 ms (-74%)、Run a 21 分達成、events=0 のまま F276 移管 |

### 2-2. 関連タスクメモ (~/fire-vault/02_todo/)

| ファイル | 主要発見 |
|---|---|
| `F276_events達成_positions_seeding.md` | 3 サブスコープ A/B/C + 期待 score 寄与 + 工数 2-3 日 |
| `F104_指数四本値取得.md` | 「未着手」、collector 本体 features/regime.py は既実装 |
| `F040_Backtest_Mode_Stage_0.md` | F040 完了済 (20 PASS)、seeding 用途への拡張余地 |
| `F275_SimilarityEngine_最適化.md` | F275 完了状態 |
| `F277_paper_live例外伝播_マルチプロセスcache.md` | F277 完了済 (commit fa771a8)、本 F276 と独立 |

### 2-3. 関連 RTM 要件

| 要件 ID | 章 | 本文ハイライト |
|---|---|---|
| **R-32-01** | 第 32 章 | サンプル数: 20 営業日以上 OR 50 トレード以上。**※Mac mini 常時稼働を活かし、過去データの高速リプレイ検証とリアルタイム Paper Live を並列実行することで、カレンダー日数を待たずに条件クリア可能。過去データで 20 営業日以上 かつ 50 トレード以上 の基準を満たせば、件数と期間の昇格条件をクリアしたものとみなす。** |
| R-19-08 | 第 19 章 | 本番移行前必須条件 6 項目 (R-32-01 と同等内容) |
| R-13-08 | 第 13 章 | 承認対象: スコア配点変更 / 候補抽出条件変更 / 利確損切条件変更 / ロット調整 / 停止条件 / **エッジ定義変更** / ダッシュボード閾値変更 → **Layer 3 active 拡大は「エッジ定義変更」相当、Fujiwara 承認必須** |
| R-14-02 | 第 14 章 | 指数データソース (F104 連携元) |

### 2-4. F271 v1.2 §6 アンチパターン該当 (16 件中 4 件)

- **§6-7 (線形外挿楽観視回避)**: F276 todo の score 寄与推定 (+0.10〜+0.15 / +0.02〜+0.04 / +0.03〜+0.06) は線形外挿のリスクあり、Phase 2 で **実測検証必須** (F275 で確立した手法を踏襲)
- **§6-8 (計測対象取り違え)**: events_total を SimilarityEngine 単独でなく **Run a 経由 (運用経路)** で計測必須、F275 と同じ規律
- **§6-13 (3 段階完了水準)**: 「動いた / 機能した / 期待値達成」を区別、events_total ≥ 50 単独でなく **R-32-01 7 項目全確認** が「期待値達成」相当 (F053 で実装済)
- **§6-20 (combination viewpoint、F300-race で新設候補)**: Phase 2-A/B/C の DAG + 共有 write 経路の組合せ観点を Phase 1 inventory に明記、§6-20 の事例化として F276 で実践

### 2-5. F271 v1.2 §3.6 パターンライブラリ該当 (14 件中 3 件)

- **§3.6.5 (DB schema migration、idempotent)**: Phase 2-B (F104 index_data テーブル追加) で適用、F300-race の `_ensure_notification_table` 自動 migration と同型パターン推奨
- **§3.6.10 (cache invalidation)**: Phase 2-A 完了後の `reset_trade_stats_cache()` 呼出 (writer-side、F275 で確立済)
- **§3.6.12 (production CLI exit code)**: Phase 2 で新規追加する CLI (F104 fetch / seeding wrapper) の exit code 一貫性、F300 hardening と整合

---

## 3. ~/fire コードベース確認結果

### 3-1. Phase 2-A 流用資産 (positions seeding)

| ファイル | 役割 | 流用可否 |
|---|---|---|
| `simulation/backtest/runner.py` (243 行) | BacktestRunner、期間指定で集計実行 + DB 記録 | ✅ 流用可、seeding mode 拡張で対応 |
| `simulation/backtest/aggregator.py` (355 行) | 5 カテゴリ集計 | ✅ 流用 (positions write は別経路、aggregator は read-only) |
| `simulation/backtest/cli.py` (91 行) | argparse CLI | ✅ 拡張: --seed-positions オプション追加 |
| `simulation/backtest/models.py` (74 行) | dataclass (BacktestRun / Result / FillModel) | ✅ 流用 |
| `simulation/replay/parallel.py` | 高速リプレイ並列実行 (F045) | ✅ 過去 20 営業日リプレイ用 |
| `scripts/seed_pattern_layer1.py` | 既存 seeding script | ⚠️ パターン用、positions 用には不可 |

### 3-2. Phase 2-A 新規実装範囲

- **新規**: `scripts/jobs/run_backtest_seeding.py` (F040 を seeding 用途で起動するラッパー)
- **拡張**: `simulation/backtest/runner.py` に「seeding mode」追加 (label 付き仮想 fill → `paper_live_positions` に INSERT)
- **DB 書込**: `paper_live_positions` テーブルに過去 6 ヶ月 × Core500 の仮想エントリー → TP/SL/timeout で仮想クローズ
- **cache invalidation**: writer-side で `reset_trade_stats_cache()` 呼出 (F275 で確立済)

### 3-3. Phase 2-B 流用資産 (F104 regime collector 解禁)

| ファイル | 役割 | 流用可否 |
|---|---|---|
| `features/regime.py` (242 行) | **`RegimeFeatureCollector` クラス + `classify_market_regime()` 既実装** | ✅ collector 本体は実装済、ready=True 化のみ必要 |
| `market_data/historical.py` | F100 historical fetcher | ✅ 同型パターンで index_fetcher 新規追加 |
| `market_data/client.py` | J-Quants V2 client (aiohttp) | ✅ 流用、`/markets/topix` 等の API 追加 |

★重要発見: **F104 = 「J-Quants 指数四本値取得 + DB schema」のみ未実装**。
collector 本体 (regime 分類ヒューリスティクス含む) は F022 完了時に既実装済 (features/regime.py:170-221)。

### 3-4. Phase 2-B 新規実装範囲

- **新規**: `market_data/index_fetcher.py` (J-Quants `/markets/topix` 等のラッパー)
- **新規**: DB schema 拡張 `data/migrations/` または既存 `scripts/setup/` に index_data テーブル
  (date / nikkei_close / nikkei_change_pct / topix_close / topix_change_pct 等)
- **修正**: `features/regime.py:173-221` の `RegimeFeatureCollector.collect()` で snapshot 取得経路を有効化 (現在 ready=False で skipped)
- **cron 登録 (Fujiwara 手動)**: 日次指数取得ジョブ (営業日朝の引け後)

### 3-5. Phase 2-C 流用資産 (Layer 3 active 拡大)

| ファイル | 役割 | 流用可否 |
|---|---|---|
| `patterns/store.py:425-440` | `transition()` で `approved_active` 昇格 | ✅ 流用 (運用のみ) |
| `patterns/rank_advisor.py` | F029 S-X 格付け自動判定 | ✅ A 完了後の再評価で使用 |
| `patterns/store.py:386` | `active_priority_set_count` 集計関数 | ✅ 監視用 |

### 3-6. Phase 2-C 新規実装範囲

- **新規実装ゼロ** (運用フェーズのみ)
- A 完了後に `rank_advisor.run()` 等で S/A 判定された pattern_id 候補を生成
- Fujiwara 承認制で 5-10 件を `transition(status="approved_active", new_rank=...)` で昇格

### 3-7. Run a 再走時の events ≥ 50 計測経路

| ファイル | 出力 |
|---|---|
| `simulation/paper_live/scheduler.py:142-211` | `events_total` 集計 + JSON 出力 |
| `simulation/paper_live/runner.py:362` | `n_events_total = sum(event_counts.values())` |
| `simulation/paper_live/cli.py:181` | 「総イベント数」print 出力 |
| `simulation/paper_live/batch_replay.py:309` | `n_events` per run + 集計 |

→ Run a 経由で events_total が JSON / DB / stdout で取得可能。F271 §6-8 (計測対象取り違え) 回避済。

### 3-8. DB 実態確認 (2026-05-05 時点)

```
patterns table:
  approved_active   1 件 (現状の Layer 3、material_initial-strong_market-breakout-AM-highliq@v1.0)
  candidate      4,708 件 (Layer 1)
  ----
  合計           4,709 件

paper_live_positions: 0 件 (Phase 2-A 着手前、空)
backtest_runs:        1 件 (2026-01-01 ~ 2026-04-30、F040 過去 1 run 履歴)
```

→ Phase 2-A の作業対象 (paper_live_positions seeding) は完全空状態、新規 INSERT のみ。
   F040 過去 1 run は seeding 履歴ではなく F040 動作確認用、流用不可。

---

## 4. Phase 2-A/B/C 構成判断表

### 4-1. 各 Phase の依存関係 (DAG)

```
Phase 2-A (positions seeding) ─┐
                                ├─→ Phase 2-C (Layer 3 拡大、A 後の S-X 再評価)
Phase 2-B (F104 regime) ────────┘   ↓
                                  Run a 再走 (A+B+C 全完了後)
                                    ↓
                                  events_total ≥ 50 確認
```

### 4-2. 表 (本部判断材料)

| Phase | 内容 | 期待 score 寄与 | events 寄与定量推定 | 新規実装範囲 | Codex リスク | 並走可否 |
|---|---|---|---|---|---|---|
| **2-A** | positions seeding (F040 拡張、過去 6 ヶ月 × Core500) | win_rate / expected_value: default 0.5 → 実 0.6-0.8、score **+0.10〜+0.15** | 高 (主要因解消) | seeding script + runner.py 拡張 | 中 (paper_live_positions 大量 INSERT、ただし単発実行で race リスク低) | B と並走可 / C と直列 |
| **2-B** | F104 (J-Quants 指数四本値 + collector 解禁) | regime_fit: default 0.5 → 実 0.7-0.9、score **+0.02〜+0.04** | 中 (補助因子) | index_fetcher + index_data テーブル | 低 (aiohttp F100 同型、レート制限あり) | A と並走可 / C と独立 |
| **2-C** | Layer 3 active 拡大 (5-10 件、Fujiwara 承認) | priority_boost: 0.1867 → 0.5-0.8、score **+0.03〜+0.06** | 中 (priority 強化) | **新規実装ゼロ** (運用のみ) | 低 (rank_advisor 既実装、運用) | A 完了後逐次 |

### 4-3. 合計期待効果 (F276 todo § 合計期待効果より)

3 Phase 完了で score: 0.5787 → 0.7-0.85 → **decision="execute" 到達** (SCORE_THRESHOLD_EXECUTE=0.65 突破) → events_total ≥ 50 達成見込み。

⚠ §6-7 (線形外挿楽観視回避) 適用: 上記寄与は **線形寄与の単純加算** で算出されており、相互作用や非線形効果は未検証。Phase 2 各段階で実測検証必須。

### 4-4. 共有書込経路の有無

- A: `paper_live_positions` (大量 INSERT) + `reset_trade_stats_cache()` 呼出
- B: `index_data` (新規テーブル) + `features` テーブル拡張 (regime カテゴリ)
- C: `patterns` の `status` UPDATE (approved_active 昇格)

**A と B の共有書込経路は無し** (別テーブル) → 並走時の race condition リスクなし。
features 書込は A/B 共に `extract_features` 経由のみ (F277 cache invalidation 機構で保護)。

---

## 5. 本部 Q 4 件の判断根拠

### Q1: events ≥ 50 解釈と Stage 3 開始日逆算

**要件 R-32-01 本文 (第 32 章)**:

> サンプル数: 20 営業日以上 または 50 トレード以上 (※Mac mini の常時稼働を活かし、
> 過去データの高速リプレイ検証とリアルタイム Paper Live を並列実行することで、
> カレンダー日数を待たずに条件クリア可能とする。**過去データで 20 営業日以上 かつ
> 50 トレード以上 の基準を満たせば、件数と期間の昇格条件をクリアしたものとみなす。**)

#### シナリオ別 Stage 3 開始日逆算

| シナリオ | 解釈 | F276 完了日 | Stage 3 開始日 |
|---|---|---|---|
| **a (Paper Live 蓄積必須、※注記不採用)** | 「20 営業日 OR 50 トレード」を OR 解釈、ただし過去データではなくリアルタイム Paper Live のみ蓄積 | 5/8-5/9 (F276 完了) → Paper Live 20 営業日蓄積 → 6/3-6/8 | **6/3-6/8 (5-7 月)** |
| **b (※注記採用、過去データ AND 条件、★Mac mini 推奨)** | R-32-01 ※注記通り「過去データで 20 営業日 かつ 50 トレード」、F276 完了即通過 | 5/8-5/9 (F276 完了) | **5/10-5/13 標準** |
| c (中間案、過去データ OR 条件) | ※注記の「かつ」を「または」に解釈、50 トレードのみ達成で通過 | 5/8 (F276 完了、Run a 1 日分達成のみ) | **5/9-5/10** |

#### Mac mini 推奨

★ **シナリオ b** (※注記採用、過去データ AND 条件、Stage 3 開始 5/10-5/13)

根拠:
- R-32-01 ※注記の文言「**過去データで 20 営業日以上 かつ 50 トレード以上**」が AND 条件として明示的
- 「Mac mini 常時稼働 + 過去データ高速リプレイ」が FIRE のエッジ定義 (再現性優位戦略、CLAUDE.md「FIRE の戦略的強み」セクション) と整合
- Stage 飛ばし禁止 (R-32-03) との整合性、AND 条件で厳格判定
- シナリオ a は要件書 ※注記を無視する解釈で不可、シナリオ c は ※注記の「かつ」を「または」と読み替える曲解で不可

### Q2: Phase 2-A/B/C 並走可否

#### 依存 DAG

§4-1 の通り: A と B は独立 (write テーブル別、共有書込なし)、A と B は並走可、C は A 後逐次 (positions 蓄積依存の S-X 再評価)。

#### 共有書込経路

§4-4 の通り: 別テーブル、競合なし。features 書込は F277 cache invalidation 機構で保護済。

#### Mac mini 推奨

★ **A/B 並走、C は A 後逐次**

工数見積:
- A: 1 日 (F040 拡張 + 過去 6 ヶ月実行)
- B: 1 日 (F104 fetcher + collector 解禁)
- A/B 並走で実 1 日完了
- C: 0.5 日 (運用のみ、A 後)
- Run a 再走 + 検証: 0.5 日

**並走採用で実工数 2 日** (本部見積 2-3 日内、シナリオ b の 5/10-5/13 標準と整合)

⚠ 並走時の注意: A の seeding と B の index_fetcher は別 process で並列実行可能だが、両者とも `extract_features` を経由するため、Codex pre-commit で「並行 INSERT による race condition」検出の可能性あり (F300-race 同型) → 並走時は **時間帯を分離** (例: A=午前、B=午後) する運用ガード推奨。

### Q3: Layer 3 active 拡大シンボル候補リスト + リスクパラメータ範囲

#### R-13-08 適用境界

R-13-08 (承認対象ルール、第 13 章): 「**エッジ定義変更**」 = Layer 3 active 拡大はこのカテゴリ → Fujiwara 承認必須。

#### 現状 + 拡大候補

現状 (DB 実態 §3-8): approved_active 1 件 (`material_initial-strong_market-breakout-AM-highliq@v1.0`、symbol=TEST_E2E、rank=B、detected_at=2026-05-01)

拡大対象生成: A 完了後 → `rank_advisor.run()` 等で S/A 判定、Fujiwara 承認下で `transition(status="approved_active", new_rank=...)` で昇格。

#### Mac mini 推奨 リスクパラメータ範囲 (素案、Fujiwara 承認待機)

| パラメータ | 推奨範囲 | 根拠 |
|---|---|---|
| 件数上限 | **5-10 件** | F276 todo §C 想定値、要件書 「Active Priority Set 3-15 件」(store.py:8) |
| rank 制約 | **S または A** | F029 格付け、B 含めるかは Fujiwara 判断 |
| 銘柄分散 | **セクター 3 種以上** | concentration risk 回避 (R-13-08 エッジ定義の独立性) |
| detected_at | **過去 6 ヶ月以内** | 古いパターン凍結 (DEATH NOTE 候補から除外) |
| min_similar_count | **>= 5 件 (R-19-08 信頼区間)** | F029 格付けの統計的有意性確保 |

### Q4: Run a 再走判定基準

#### F276 完了判定 = events_total ≥ 50 単独 vs 補助指標併用

R-32-01 (Paper→Semi 昇格条件 7 項目) との関係:

| 項目 | F276 主目的 | F053 で別途確認 |
|---|---|---|
| 1. サンプル数 (20 営業日 ∧ 50 トレード) | ✅ Run a で達成 | F053 sample_size |
| 2. 主戦略の期待値プラス | ⚠ Run a で達成可、ただし win_rate マイナスなら蹴られる | F053 expected_value |
| 3. 最大ドローダウン | △ Run a で観測 | F053 で別途 |
| 4. Simulation Optimistic Bias Score | △ Run a で観測 | F053 accuracy |
| 5. 強制クローズ成功率 | × Paper Live 必要 | F053 close |
| 6. 停止機能の動作確認 | × Paper Live 必要 | F053 halt |
| 7. Evaluation Agent 確認 | × 別途 | F119 |

#### Mac mini 推奨

★ **F276 完了判定 = events_total ≥ 50 単独**

ただし F276 完了 = Stage 3 移行可ではない。F053 promotion check 5/5 PASS が別途必要 (F241 で実装済 9 項目チェック)、Stage 3 移行 (F266) は F276 + F053 + LINE 5 部屋疎通 + Fujiwara 承認の AND。

根拠:
- R-32-01 の 7 項目 全網羅は F053 で実装済 (前回 F241 で 9 項目チェック確立、Stage 3 移行ゲート 1 段階クリア = v0.2-stage3-wrapper-ready tag に統合)
- F276 主目的は events_total ≥ 50 (項目 1) のみ、他 6 項目は F276 後の F053 走査で確認
- F276 完了基準を補助指標併用にすると scope 拡大 (F261-fix 系の負のループ再発リスク、HQ-Lean v1.1 ルール 6 候補)

#### Run a で win_rate がマイナスだった場合の対応 (Phase 2-D 候補、Phase 1 では起票せず scope 拡大で対応)

- 対応 1: Layer 3 active 拡大候補の選定基準で win_rate プラス pattern のみに絞る (Q3 Mac mini 推奨に「win_rate >= 0」追加)
- 対応 2: SCORE_THRESHOLD_EXECUTE 0.65 → 0.55 に微調整 (要 R-13-08 Fujiwara 承認、エッジ定義変更相当)
- 対応 3: Phase 2-D 起票せず、F276 完結後の F119 Evaluation Agent 提案で対応

---

## 6. F271 v1.2 §6 / §3.6 / 観点 v3 該当事項のまとめ

### 6-1. §6 アンチパターン 16 件 該当 (4 件)

| 番号 | 内容 | F276 での適用 |
|---|---|---|
| §6-7 | 線形外挿楽観視回避 | score 寄与推定 (+0.10/+0.04/+0.05) は線形外挿、Phase 2 各段階で実測検証必須 |
| §6-8 | 計測対象取り違え | events_total を Run a 経由 (運用経路) で計測、SimilarityEngine 単独不可 |
| §6-13 | 3 段階完了水準 | 「動いた / 機能した / 期待値達成」区別、F276 = 「機能した」、「期待値達成」は F053 で |
| §6-20 (新設候補) | combination viewpoint | A/B/C の DAG + 共有 write 経路を Phase 1 inventory に明記、F300-race の §6-20 候補事例化 |

### 6-2. §3.6 パターンライブラリ 14 件 該当 (3 件)

| 番号 | 内容 | F276 での適用 |
|---|---|---|
| §3.6.5 | DB schema migration、idempotent | Phase 2-B (F104 index_data テーブル追加)、F300-race `_ensure_notification_table` 自動 migration と同型 |
| §3.6.10 | cache invalidation | Phase 2-A 完了後の `reset_trade_stats_cache()` 呼出 (writer-side、F275 で確立済) |
| §3.6.12 | production CLI exit code | 新規 CLI (F104 fetch / seeding wrapper) の exit code 一貫性、F300 hardening と整合 |

### 6-3. 観点 v3 7 観点 (Phase 1 grep 適用結果)

| 観点 | 結果 |
|---|---|
| 1. 設計記録の有無 | ✅ F276 todo / F275 complete / F040 design 全有 |
| 2. 過去 commit / handover | ✅ F274/F275/F267 commit + handover あり |
| 3. 関連 RTM | ✅ R-32-01 / R-19-08 / R-13-08 / R-14-02 |
| 4. 既存実装ファイルパス | ✅ simulation/backtest/, features/regime.py, patterns/rank_advisor.py |
| 5. 依存タスク完了状態 | ✅ F040 / F100 / F101 / F275 すべて完了 |
| 6. 既知の制約 / アンチパターン | ✅ §6-7/6-8/6-13/6-20 (上記) |
| 7. combination viewpoint (§6-20) | ✅ A/B 並走 + C 逐次の DAG + 共有 write 経路評価済 |

---

## 7. 本部判断要請 (Phase 2 着手前)

### Q1: events ≥ 50 解釈

★ Mac mini 推奨: **シナリオ b** (※注記採用、過去データ AND 条件、Stage 3 開始 5/10-5/13)

### Q2: Phase 2-A/B/C 並走可否

★ Mac mini 推奨: **A/B 並走、C は A 後逐次** (実工数 2 日)

### Q3: Layer 3 active 拡大シンボル候補 + リスクパラメータ範囲

★ Mac mini 推奨: 件数 5-10 件 / rank S-A / セクター 3 種以上 / detected_at 過去 6 ヶ月 / min_similar_count >= 5

### Q4: Run a 再走判定基準

★ Mac mini 推奨: **F276 完了判定 = events_total ≥ 50 単独**、補助指標は F053 で別途 (R-32-01 7 項目 網羅は F241 で実装済)

---

## 8. 完了条件チェック (F271 v1.1 完了水準 3 段階)

| 段階 | 状態 | 根拠 |
|---|---|---|
| 動いた | ✅ | Vault grep + ~/fire コードベース確認 + DB 実態確認すべて完走 |
| 機能した | ✅ | Phase 2 構成判断材料 (§4) + 本部 Q 4 件根拠 (§5) 整理済 |
| 期待値達成 | ✅ | Q 4 件確定後即 Phase 2 着手指示文発行可能な情報密度、Mac mini 推奨明示 |

---

## 9. 制約遵守確認

- ✅ 同一番号系列内起票分割禁止 (HQ-Lean v1.1 ルール 6 候補): F276-design / F276-investigation 等への分離なし、本 Phase 1 内で完結
- ✅ Codex pre-commit 厳格運用: 本 Phase はコード変更なし、Vault commit のみ (Vault 側に Codex hook なし)
- ✅ 実装 commit なし、Vault handover ファイル作成のみ
- ✅ Vault 出力先: `~/fire-vault/03_design/F276_phase1_2026-05-05.md` (本ファイル)

---

## 10. 参照リンク

- F276 todo: [[../02_todo/F276_events達成_positions_seeding]]
- F275 完了: [[F275_similarity_optimization_complete_2026-05-04]]
- F275 Phase 1 design: [[F275_phase1_design_2026-05-04]]
- F040 status: [[F040_backtest_status_2026-05-04]]
- F104 todo: [[../02_todo/F104_指数四本値取得]]
- F271 v1.2: [[F271_v1.2]]
- 完了基準: [[task_completion_criteria]]
- 第 32 章 R-32-01: [[../01_requirements/FIRE_要件書_第32章_本番移行基準の厳密化_穴10対策_]]
- 第 13 章 R-13-08: [[../01_requirements/FIRE_要件書_第13章_Evaluation_Agent_と_Dashboard]]
- F300 + F300-race 完結 (Stage 3 ゲート 1 段階): log.md 2026-05-05 milestone エントリ
